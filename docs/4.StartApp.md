
# Локальная сборка образа

```
docker buildx build --platform linux/arm64 -t tutor_bot_test:develop --load .
docker save tutor_bot_test:develop  | bash -c "dd bs=4M status=progress" | ssh gtr@192.168.0.221 "docker load"

docker build -t urfu_test:v1 . 
docker save urfu_test:v1 | ssh gtr@192.168.0.221 "docker load"
docker save urfu_test:v1 | pv | ssh gtr@192.168.0.221 "docker load"


apt-get update && apt-get install -y file
file /usr/share/dotnet/dotnet
```


```
watch -n 2 'echo "CPU: $(top -bn1 | grep '"'"'Cpu(s)'"'"' | awk '"'"'{print $2}'"'"')% | RAM: $(free -m | awk '"'"'NR==2{printf "%.1f%%", $3*100/$2}'"'"') | Temp: $(vcgencmd measure_temp)"'
```